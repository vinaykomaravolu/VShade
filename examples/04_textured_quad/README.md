# Textured Quad

This example loads `player.png` through `AssetManager::loadResource`, creates an
orthographic camera, and submits one textured quad through `Renderer2D`.
Asset resources are cached and owned through `AssetRef`/`shared_ptr`. Next:
`05_camera_2d`.
