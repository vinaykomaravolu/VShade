# Instancing — planned

VShade does not yet expose GPU instance buffers or instanced draw submission.
This directory intentionally contains no misleading workaround. Add the demo
when `Renderer3D` has a real instancing API and statistics.
