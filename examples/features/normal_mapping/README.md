# Normal Mapping

Load `eye.glb`, whose imported materials include tangent-space normal maps.
The model loader imports tangents and normal textures; `Renderer3D` binds the
neutral fallback or imported normal map automatically.
