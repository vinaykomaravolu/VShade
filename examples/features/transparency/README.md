# Transparency

Overlap translucent quads and control their order with sorting layers. For 3D
content, use `MaterialAlphaMode::Mask` or `Blend` and `setDoubleSided()` when
the source material requires it.
