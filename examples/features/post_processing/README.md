# Post Processing — planned

VShade has framebuffers but no fullscreen render-pass/post-process abstraction.
This example will follow the custom render-pass API rather than embedding raw
OpenGL calls in a tutorial.
