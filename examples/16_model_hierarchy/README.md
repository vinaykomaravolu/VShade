# Model Hierarchy

Load a multi-mesh glTF model, inspect its mesh/node counts, and draw it with one
call. `Model` validates acyclic nodes and composes parent/child transforms while
submitting primitives. Next: `17_physics_2d`.
