# Physics Collisions

Configure collision layers/masks, mark a collider as a sensor, and receive
scene-level contacts containing entities rather than backend body IDs. Events
are queued by the backend and dispatched safely after the physics step. Next:
`19_audio`.
