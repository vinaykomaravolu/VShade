# Camera 2D

Move the orthographic camera with WASD and zoom with the wheel. The example
draws a large checkerboard of colored tiles with a red world-origin marker, so
translation and zoom are easy to see. It updates projection bounds for zoom
and uses `Camera::lookAt` for translation. Next: `06_renderer_2d`.
