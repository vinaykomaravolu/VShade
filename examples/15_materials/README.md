# Materials

Create lit materials with distinct albedo, metallic, and roughness values.
`Material` also supports albedo textures, tangent-space normal maps, alpha
modes, alpha cutoff, double-sided rendering, and custom shader parameters.
Next: `16_model_hierarchy`.
