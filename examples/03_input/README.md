# Input

Press Space to change the background, click the left mouse button, scroll, and
move the mouse. The console shows pressed events, cursor position, per-frame
mouse delta, and scroll delta. Escape exits.

Use `isKeyDown` for held state and `isKeyPressed`/`isKeyReleased` for one-frame
edges. Mouse buttons expose the same three states. Next: `04_textured_quad`.
