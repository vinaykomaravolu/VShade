# Audio 3D

Declare one active listener and a looping spatial source. Moving the source
entity updates its voice position; attenuation uses configurable minimum and
maximum distances. Scene voices stop with the scene. Next: `21_asset_manager`.
