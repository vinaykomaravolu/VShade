# Camera 3D

Move with WASD, hold the right mouse button to look, and scroll to change
movement speed. `CameraController` owns controller state while updating an
ordinary `Camera`. Next: `13_lighting`.
