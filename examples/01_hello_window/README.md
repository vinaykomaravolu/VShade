# Hello Window

## What you'll learn

- Deriving from `vshade::Application`
- Clearing the framebuffer in `onRender()`
- Closing an application from input

`Application` creates the window and renderer before `onStart()`, runs the
variable/fixed frame loop, and shuts everything down automatically. Press
Escape to exit. Next: `02_triangle`.
