# Scene Entities

Create named entities, use the transform every entity owns, attach a custom
component, and iterate an EnTT-compatible scene view. `Entity` also offers
`set`, `get`, `tryGet`, `has`, and `remove`. Next: `08_sprite_scene`.
