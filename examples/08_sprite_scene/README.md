# Sprite Scene

This is the first component-driven rendering example. A camera and sprite are
declared as entities, then `playScene(scene)` lets `SceneRuntime` select the
camera, resolve the texture, and render automatically. No `onRender()` override
is needed. Next: `09_scene_serialization`.
