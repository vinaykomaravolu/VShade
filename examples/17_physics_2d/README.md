# Physics 2D

Add `RigidBody2DComponent` and `Collider2DComponent` to scene entities. The
runtime creates Box2D bodies, advances them at the fixed timestep, and writes a
dynamic body's position back to its transform before rendering. Next:
`18_physics_collisions`.
