# Asset Manager

Load the same texture twice to observe one cached resource, then create a typed
model `AssetReference` without loading it. References combine a stable handle
and normalized source path so scenes can resolve them in a fresh process.
Next: `22_scripting`.
