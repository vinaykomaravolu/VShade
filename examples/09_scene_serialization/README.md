# Scene Serialization

Create a scene, save it as readable JSON, load it transactionally into another
scene, and locate the entity by its preserved UUID. Structured
`Result<void>/Diagnostic` reports recoverable content errors. The generated
file is under the example build directory. Next: `10_framebuffer`.
